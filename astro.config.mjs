import { defineConfig } from 'astro/config';

export default defineConfig({
  // Set this to your real domain before going live (used for sitemap + canonical URLs)
  site: 'https://theitinerarywala.com',
});
